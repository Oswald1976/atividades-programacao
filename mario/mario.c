#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int s = 0
    int b = 0
    int a = get_int ("Digite a altura da piramide que deseja(aceitavel entre 1 e 8)");
    while ( a > 0 || a < 9 )
        {
            for (int i = 0; i < a; i++)
            {
                for (int j = 0; j < a; j++)
                {
                    for (int l = 0; l < a;l++)
                    {
                    /* do
                       {
                        printf("#");
                        }
                        while( j <= a );
                        */
                    }
                }
            printf("\n");
            }

        }
             loop

    
}