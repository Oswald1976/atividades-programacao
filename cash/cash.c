#include <cs50.h>
#include <stdio.h>

float cent;
float b = 0.25;
float c = 0.10;
float d = 0.05;
float e = 0.01;

int main(void)
{
    do
        cent = get_float("Digite o Valor que terá que dar de troco em moedas (aceitavel entre 0,01 e 0,99)");
    while (a < 0, 01 || a > 0, 99);
    if (a >= 0, 25)
    {
    }
}